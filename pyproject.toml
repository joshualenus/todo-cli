[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "taskapp"
version = "0.1.0"
dependencies = [
    "typer",
    "rich",
]

[project.scripts]
todo = "taskapp.cli:app"

[tool.setuptools.packages.find]
include = ["taskapp*"]